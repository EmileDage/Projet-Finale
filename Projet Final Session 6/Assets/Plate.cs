using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Plate : Comptoire
{
    //peut etre lie ca au client so selon ce que le client veut le plate refuse ou accpete la bouffe depose

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
